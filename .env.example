NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Leave unset for production. When Supabase env vars are missing,
# the app runs a local demo mode so the MVP can be reviewed immediately.
NEXT_PUBLIC_DEMO_MODE=true
